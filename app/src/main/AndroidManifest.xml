<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inhamind"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Secession"></activity>
        <activity android:name=".MyPage" />
        <activity android:name=".SMSPswdFind" />
        <activity android:name=".EmailPswdFind" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".PswdRemake" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PswdFind" />
        <activity
            android:name=".makeAccountActivity"
            android:label="@string/title_activity_make_account"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>